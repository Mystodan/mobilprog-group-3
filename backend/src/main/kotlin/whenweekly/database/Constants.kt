package whenweekly.database


object Constants {
    const val DATABASE_URL = "database.config.url"
    const val DATABASE_USERNAME = "database.config.username"
    const val DATABASE_PASSWORD = "database.config.password"
}